# OPMAS-001 | React Environment
# Copy to .env.local — never commit .env.local

# API base URL — change to real server URL when deployed
REACT_APP_API_URL=http://localhost:8000/api

# WebSocket server (Soketi or Pusher)
REACT_APP_WS_HOST=localhost
REACT_APP_WS_PORT=6001
REACT_APP_WS_KEY=opmas-key
